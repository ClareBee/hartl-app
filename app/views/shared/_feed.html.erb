<% if @feed_items.any? %>
  <ol class="microposts">
  <%# Rails knows to render _micropost partial as @feed_items each have class of Micropost %>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>