<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class=users>
  <!-- Rails automatically iterates and renders each w partial called _user  -->
  <%= render @users %>
</ul>

<%= will_paginate %>
